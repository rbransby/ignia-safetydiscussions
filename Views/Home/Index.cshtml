@model List<User>

<h1> Select a user to view safety discussions for: </h1>

@foreach (var user in Model)
{	
	<div>
		<a asp-route="ViewSafetyDiscussionByUser" asp-route-userId="@user.Id">@user.DisplayName</a>
	</div>
}